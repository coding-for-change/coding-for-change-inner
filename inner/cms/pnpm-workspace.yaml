# pnpm 10+ reads overrides from here, not from the package.json `pnpm` field.
overrides:
  '@payloadcms/drizzle': 3.79.1

# Allowlist for pnpm's opt-in postinstall-script security model.
onlyBuiltDependencies:
  - esbuild
  - sharp
